The top rule of the conversation panel: peer name left, state right, an optional ← back control on narrow screens.

```jsx
<ChatHeader peer="aisha@example.com" status="Connected" onBack={close} />
```

Separated from the messages by a hairline, never a filled bar.
