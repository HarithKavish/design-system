A single-field form where the submit control lives inside the field — used to start a conversation from an email address.

```jsx
<InputWithButton type="email" value={peer} onChange={setPeer} onSubmit={start} placeholder="Enter peer email ID" />
```

The submit affordance is the → character in accent colour, not an icon asset.
