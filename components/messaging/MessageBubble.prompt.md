A chat message. Own messages are an accent-tinted surface on the right; the peer's are the raised surface on the left. Both keep the hairline border.

```jsx
<MessageBubble from="me" timestamp="09:41">Sent from the shared theme.</MessageBubble>
```

Max width 72%. No tails, no gradients — the migration from the v1 glass bubbles dropped both.
