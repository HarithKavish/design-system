The signed-in identity block in an app sidebar. Its dashed border marks it as state rather than content — the one dashed edge in the system.

```jsx
<UserCard name="Harith Kavish" email="hello@harithkavish.com" avatarSrc={picture} />
```

Renders its own empty state when `name` is absent.
