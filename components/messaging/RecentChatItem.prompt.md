A row in the recent-conversations list. Without an avatar it falls back to a tinted initial disc rather than a placeholder image.

```jsx
<RecentChatItem name="Aisha" email="aisha@example.com" active onClick={open} />
```

Selected state is an accent border on the raised surface.
