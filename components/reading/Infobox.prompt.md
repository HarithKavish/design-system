A 17.5rem summary card in its own column beside the article, so the reading measure never shrinks to accommodate it.

```jsx
<Infobox rows={[{ label: 'Type', value: 'Field note' }, { label: 'Updated', value: '2026-03-09' }]} />
```

Rows are separated by hairlines; labels are small uppercase, values semibold. It stacks above the article below 1040px.
