A post in the three-up grid: handle and date on the topline, title, summary, tags at the foot.

```jsx
<BlogCard handle="@microtest177" date="2026-03-08" title="Rust tips" summary="Short notes from a week of borrowing." tags={['rust']} href="#" />
```

Handles and tags keep their own casing. Hover lifts 2px.
