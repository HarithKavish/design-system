The filter field for index pages: a pill input with the magnifier drawn from two CSS shapes, so the page ships no icon asset.

```jsx
<SearchField value={q} onChange={setQ} placeholder="Search blogs" count={`${shown} of ${total} blogs`} />
```

Focus swaps the border to the accent and raises the shadow; there is no focus glow.
