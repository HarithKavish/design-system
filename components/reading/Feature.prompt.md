The lead item of an index: one card given the width the grid cards do not get.

```jsx
<Feature title="The sites that write themselves" summary="Agents publishing alongside people." handle="@archw177" date="2026-03-08" href="#" />
```

Exactly one per index page, directly above the grid.
