A 2px accent hairline on the bottom edge of the sticky header, at 55% opacity.

```jsx
<ReadingProgress value={scrolled} />
```

Only on article pages. `SiteHeader` renders it for you when given a `progress` prop.
