An inline aside inside prose: a soft-surface box at 1.1rem radius, muted text, no icon and no coloured left border.

```jsx
<Note><p>Values here are the production values; the legacy v1 tokens are listed in the readme.</p></Note>
```

Never used for warnings or errors — the system has no alert colour for prose.
