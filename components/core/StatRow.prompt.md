A single ruled band of counts, sitting on the page background rather than on cards — the numbers are context for the page, not its subject.

```jsx
<StatRow stats={[{ value: '42', label: 'Blogs' }, { value: '7', label: 'Authors' }]} />
```

Values are tabular-nums. Never boxed as four separate cards.
