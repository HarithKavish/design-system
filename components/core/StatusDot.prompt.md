A 0.62em dot for live connection state inside app surfaces (presence, sync). Carried over from the v1 shell, retoned to the muted status palette.

```jsx
<StatusDot state="online" label="Connected" />
```

Marketing surfaces use Pill instead — the dot is only for state that changes while you watch.
